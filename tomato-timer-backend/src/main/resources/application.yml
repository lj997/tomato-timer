server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/tomato_timer?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai
    username: test
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.tomato.entity
  configuration:
    map-underscore-to-camel-case: true

logging:
  level:
    com.tomato: debug
